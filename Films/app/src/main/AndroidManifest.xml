<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/movies_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_home_cinema_amico"
        android:supportsRtl="true"
        android:theme="@style/Theme.Old_Filmograf"
        android:versionCode="1"
        android:versionName="initial"
        tools:targetApi="31">
        <activity android:name=".MovieActivity"/>
        <activity android:name=".AllMovieActivity" />
        <activity android:name=".PopularMovieListActivity" />
        <activity android:name=".Profile" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Old_Filmograf">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>